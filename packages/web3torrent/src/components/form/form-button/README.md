# FormButton

Represents a button in the UI.

```typescript
const button = (
  <FormButton
    disabled={false}
    type="button"
    block={false}
    spinner={false}
    onClick={() => alert('Clicked!')}
  >
    Click me!
  </FormButton>
);
```
