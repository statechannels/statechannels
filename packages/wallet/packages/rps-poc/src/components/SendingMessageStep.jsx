import React from 'react';

export default class WaitForOpponent extends React.PureComponent {
  render() {
    return (
      <div style={{ maxWidth: '90%', margin: 'auto' }}>
        <div>
          <h1>Sending message to opponent...</h1>
        </div>
      </div>
    );
  }
}
