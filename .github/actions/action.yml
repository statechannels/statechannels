# action.yml
name: headfulteer
description: 'headful puppeteer'
inputs:
  command:  # id of input
    description: 'command to run'
    required: true
    default: ''
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.command}}